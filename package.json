{
  "name": "schoolbox-api",
  "version": "1.1.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.50",
    "redoc-cli": "^0.12.1"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi validate",
    "build-static": "npm run build && redoc-cli bundle -o docs/index.html -t docs/template.html dist.yaml"
  }
}
