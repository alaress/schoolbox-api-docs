type: object
properties:
  data:
    type: array
    properties:
      courses:
        $ref: ./courseItem.yaml
      suggestion:
        $ref: ./courseItem.yaml

  metadata:
    $ref: ./listMetadata.yaml