allOf:
  - $ref: ./common.yaml
  # TODO add more than the folder's name? e.g. their parentage
  - $ref: ../../folder.yaml
  - type: object
    properties:
      type:
        type: string
        enum: [folder]
