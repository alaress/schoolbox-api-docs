allOf:
  - $ref: ./common.yaml
  - type: object
    title: User search result
    properties:
      type:
        enum: [user]
      subtype:
        enum:
          - user.guest
          - user.parent
          - user.staff
          - user.student
