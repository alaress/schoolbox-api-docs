allOf:
  - $ref: ./common.yaml
  # TODO remove some details, should need only:
  #      id, date, title, assessment type, work type, context
  - $ref: ../../assessment-item.yaml
  - type: object
    properties:
      type:
        type: string
        enum: [assessment]

