post:
  operationId: commsNews.getListForIndex
  tags: [comms]
  summary: Get articles to display on the home page
  description: |
    Get the list of articles to display on the [news home page](#operation/commsNews.getIndex).
  responses:
    '200':
      description: |
        A list of articles to display on the home page.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/news/commonFields.yaml
    default:
      $ref: ../components/responses/problem.yaml
