allOf:
  - $ref: ./common.yaml
  - type: object
    title: Assessment search result
    properties:
      type:
        enum: [assessment]
      subtype:
        enum:
          - assessment.dueWork
          - assessment.lessonPlan
          - assessment.project
          - assessment.task
          - assessment.quiz
