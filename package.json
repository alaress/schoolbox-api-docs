{
  "name": "schoolbox-api",
  "version": "1.2.9",
  "dependencies": {
    "@redocly/cli": "^1.0.0-beta.109"
  },
  "private": true,
  "scripts": {
    "build": "redocly bundle -o dist",
    "test": "redocly lint",
    "build-static": "npm run build && mv dist.yaml docs/"
  }
}
