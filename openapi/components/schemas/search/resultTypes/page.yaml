allOf:
  - $ref: ./common.yaml
  - type: object
    title: Page search result
    properties:
      type:
        enum: [page]
      subtype:
        enum:
          - page.class
          - page.course
          - page.unit
          - page.homepage
          - page.other
      subResults:
        $ref: ./page/subResult-list.yaml
