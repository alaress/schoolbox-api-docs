description: A list of search results
content:
  application/json:
    schema:
      $ref: ../../schemas/search/result-item.yaml
    examples:
      One of each:
        $ref: ../../examples/search/result.yaml
