{
  "name": "schoolbox-api",
  "version": "1.2.9",
  "dependencies": {
    "@redocly/cli": "^1.0.0-beta.109",
    "@redocly/openapi-core": "^1.0.0-beta.109"
  },
  "private": true,
  "scripts": {
    "build": "redocly bundle -o docs/builds/${npm_config_build_version}.yaml && ./scripts/addBuild.js ${npm_config_build_version}",
    "test": "redocly lint",
    "build-static": "redocly bundle -o docs/dist"
  }
}
