allOf:
  - $ref: ./common.yaml
  # TODO add more than the user's name? e.g. their role?
  - $ref: ../../userAuthor.yaml
  - type: object
    properties:
      type:
        type: string
        enum: [user]
