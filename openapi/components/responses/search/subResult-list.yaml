description: A list of search results for a page
content:
  application/json:
    schema:
      $ref: ../../schemas/search/resultTypes/page/subResult-list.yaml
    examples:
      One of each:
        $ref: ../../examples/search/page/subResult.yaml
