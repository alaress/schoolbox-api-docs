allOf:
  - $ref: ./assessment.yaml
  - type: object
    description: specific fields for Task Assessments
    properties:

      assessmentType:
        type: string

      link:
        type: string

      selfAssessable:
        type: boolean
