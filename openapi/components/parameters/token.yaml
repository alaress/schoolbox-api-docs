name:        token
in:          query
deprecated:  true
description: |
  A token for user authentication.
  This is temporary, and will be replaced with oAuth/JWT authentication.
schema:
  type: string
