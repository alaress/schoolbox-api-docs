name: descendants
in: query
description: |
  The number of levels of descendants to include in `children`. With `0`,
  `children` is `null`.
schema:
  type: integer
  format: int32
  minimum: 0
  maximum: 50
  default: 0
