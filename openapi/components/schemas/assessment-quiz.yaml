allOf:
  - $ref: ./assessment.yaml
  - type: object
    description: Specific fields for Quizes
    properties:

      assessmentType:
        type: string

      maxAttempts:
        type: string
        example: Unlimited

      allowedTime:
        type: string
        example: 2H15Min

      completionMessage:
        type: string

      section:
        type: array
        items:
          $ref: ./quiz-section.yaml