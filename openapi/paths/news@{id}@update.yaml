get:
  operationId: commsNews.getUpdate
  tags: [comms]
  summary: HTML template for news update
  responses:
    '200':
      description: A HTML template for the news update page.
      content:
        text/html:
          schema:
            type: string
    default:
      $ref: ../components/responses/problem.yaml
  parameters:
    - $ref: ../components/parameters/id.yaml
