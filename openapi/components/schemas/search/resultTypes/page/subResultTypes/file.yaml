allOf:
  - $ref: ../../common.yaml
  - type: object
    title: File search result
    properties:
      type:
        enum: [file]
      subtype:
        enum:
          - file.audio
          - file.document
          - file.image
          - file.video
          - file.other
