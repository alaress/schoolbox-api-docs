description: a list of comments
content:
  application/json:
    schema:
      $ref: ../schemas/discussionComment-readList.yaml
    examples:
      empty:
        $ref: ../examples/discussionComment-listEmpty.yaml
      one:
        $ref: ../examples/discussionComment-listOne.yaml
      many:
        $ref: ../examples/discussionComment-listMany.yaml
  text/html:
    schema:
      type: string
      description: The list of comments, rendered into HTML on the server.
