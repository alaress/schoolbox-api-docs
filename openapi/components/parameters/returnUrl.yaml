name:        returnUrl
in:          query
description: |
  A base-64 encoded URL to redirect to, once processing of the entity on this
  page is completed.
schema:
  type: string
  pattern: "^[a-zA-Z0-9+/]+={0,2}$"
