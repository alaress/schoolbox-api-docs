{
  "name": "schoolbox-api",
  "version": "1.2.9",
  "dependencies": {
    "@redocly/cli": "^1.0.0-beta.109"
  },
  "private": true,
  "scripts": {
    "build": "redocly bundle -o docs/dist",
    "test": "redocly lint",
    "build-static": "redocly bundle -o docs/dist"
  }
}
