title: A course usage
description: A course usage
readOnly: true
type: object
properties:
  discriminator:
    type: string
    enum: [course]
  object:
    type: object
    properties:
      id:
        $ref: id.yaml
      name:
        type: string
        description: The name of the course.
