allOf:
  - $ref: ./assessment.yaml
  - type: object
    description: Common fields for assessments
    properties:

      assessmentType:
        type: string

      projectChildren:
        items:
          $ref: ./assessment-read.yaml