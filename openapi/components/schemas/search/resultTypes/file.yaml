allOf:
  - $ref: ./common.yaml
  # TODO this is just storage files? this should return SBFS files
  - $ref: ../../file.yaml
  - type: object
    properties:
      type:
        type: string
        enum: [file]
